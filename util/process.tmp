# pos_classes = ['u%03d' % i for i in POS_USER_RANGE]
    # neg_classes = ['u%03d' % i for i in NEG_USER_RANGE]

    # # Select positive classes from session 1
    # X_pos, y_pos = get_X_y(df_s1, 0)

    # # Select negative classes from session 1
    # X_neg, y_neg = get_X_y(df_s1, int(X_pos.shape[0] / (SPLIT-1)))

    # # Compile training data
    # X_train = np.concatenate((X_pos, X_neg), axis=0)
    # y_train = np.concatenate((y_pos, y_neg), axis=0).astype(int)

    # # Select positive classes from session 2
    # X_pos, y_pos = get_X_y(df_s2, 0)

    # # Select negative classes from session 2
    # X_neg, y_neg = get_X_y(df_s2, int(X_pos.shape[0] / (SPLIT-1)))

    # # Compile testing data
    # X_test = np.concatenate((X_pos, X_neg), axis=0)
    # y_test = np.concatenate((y_pos, y_neg), axis=0).astype(int)

    # return X_train, X_test, y_train, y_test